[project]
name = "codec_sep"
version = "0.1.0"
description = "codec_sep"
authors = [{name = "Your Name", email = "you@example.com"}]
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"